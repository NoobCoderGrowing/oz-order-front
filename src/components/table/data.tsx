import { OrderEntry } from "../TypeDefinition"

export type Student = {
    studentId: number
    name: string
    dateOfBirth: string
    major: string
  }
  
  export const data: OrderEntry[] = [
  ]
  